# گزارش پیشرفت پروژه OCR

## تاریخ
2026-08-13

## وضعیت فعلی
پروژه از حالت مدل‌محور و وابسته به وزن‌های ناموجود خارج شده و به سمت موتور OCR واقعی و قابل‌اعتماد هدایت شده است.

## مشکل اصلی که حل شد
در بررسی اولیه، لینک‌های دانلود مدل‌های CRAFT و CRNN نامعتبر بودند و فایل‌های دانلودشده یا 404 می‌گرفتند یا برای `torch.load` با نسخه جدید PyTorch سازگار نبودند. این موضوع باعث شده بود که:

- `scripts/download_weights.py` خطا بدهد
- `inference.py` در زمان لود مدل‌ها_FAIL کند
- برنامه فقط در حالت fallback یا Dummy اجرا شود

## تصمیم اتخاذشده
به جای ادامه‌دادن روی مدل‌های ناموجود، پروژه به یک مسیر عملی‌تر و قابل‌اجرا منتقل شد:

- استفاده از موتور OCR واقعی: Tesseract
- حذف وابستگی به مدل‌های CRAFT/CRNN در زمان اجرای اصلی
- حفظ ساختار پروژه برای آینده، به‌خصوص اگر مدل‌های واقعی در آینده پیدا شوند

## فایل‌های اصلاح‌شده

### 1) `inference.py`
- وابستگی به `CRAFT` و `CRNN` حذف شد
- از `OCRBackendFactory` استفاده شد
- `OCREngine` اکنون با backend جدید و بدون نیاز به وزن‌های خراب کار می‌کند

### 2) `configs/config.yaml`
- بخش `ocr.backend: tesseract` اضافه شد
- زبان OCR به `fas+eng` تنظیم شد
- مسیرهای مدل‌های قدیمی در حالت legacy حفظ شدند اما فعال نیستند

### 3) `backend/ocr_backend.py`
- یک backend جدید به نام `TesseractOCRBackend` ساخته شد
- برای OCR روی تصاویر از `pytesseract` استفاده می‌شود
- این فایل پایه‌ی اجرا و تست پروژه را فراهم می‌کند

### 4) `scripts/download_weights.py`
- رفتار دانلود بهبود یافت
- فایل‌های نامعتبر حذف می‌شوند
- در صورت 404 یا فایل‌های HTML/نامعتبر، پیام روشن می‌دهد

### 5) `CHANGELOG_OCR_BACKEND.md`
- گزارش کامل تغییرات به صورت مستند ثبت شد

## دلیلی که این مسیر درست است
- هیچ لینک معتبر و ثابت برای وزن‌های CRAFT/CRNN در محیط فعلی پیدا نشد
- پروژه بدون مدل واقعی، اجرا می‌شود ولی OCR واقعی ندارد
- Tesseract یک موتور OCR واقعی و قابل‌اعتماد است و برای پروژه‌ی حاضر عملی‌تر است

## پیش‌نیازهای باقی‌مانده
برای اینکه پروژه کاملاً عملی و استفاده‌پذیر شود، باید این مراحل انجام شود:

1. نصب Tesseract OCR روی سیستم
2. بررسی نسخه و صحت نصب با `tesseract --version`
3. اجرای [ui.py](ui.py) روی یک تصویر واقعی
4. تنظیم بهتر زبان و PSM برای متن فارسی
5. تست PDF و API بعد از موفقیت اولیه

## نتیجه نهایی
پروژه از حالت "وابسته به مدل‌های ناموجود" به حالت "قابل اجرا و واقعی با OCR engine" منتقل شده است. اکنون لازم است فقط Tesseract نصب و یک تست واقعی روی تصویر/PDF انجام شود تا عملکرد نهایی بررسی گردد.

## یادداشت
این مسیر برای اجرای عملی پروژه بهتر از تلاش بی‌پایان برای دانلود مدل‌های خراب است. اگر در آینده مدل‌های معتبر و سازگار پیدا شوند، می‌توان دوباره مسیر مدل‌محور را بازگرداند.
