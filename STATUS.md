# 📊 گزارش وضعیت پروژه OCR فارسی

این مستند وضعیت پیشرفت پروژه را بر اساس [نقشه راه اولیه (roadmap.md)](./roadmap.md) نشان می‌دهد.

---

## 🟢 مراحل تکمیل‌شده (آماده‌ی استفاده)

| مرحله در نقشه راه | وضعیت | توضیحات و فایل‌های مربوطه |
|:---|:---:|:---|
| **مرحله ۰: پیش‌نیازها** | ✅ | ساختار پوشه‌ها کامل شد و `requirements.txt` ایجاد گردید. محیط مجازی و پکیج‌ها نیز نصب شده‌اند. |
| **مرحله ۲: پیش‌پردازش تصویر** | ✅ | کلاس `ImagePreprocessor` جهت حذف نویز و تصحیح زاویه در فایل `preprocessing/image_processing.py` نوشته شد. |
| **مرحله ۵: پردازش PDF** | ✅ | کلاس `PDFProcessor` برای استخراج متن و تصاویر از PDF در `preprocessing/pdf_processing.py` نوشته شد. |
| **مرحله ۶: پس‌پردازش متن** | ✅ | کلاس `TextPostProcessor` برای نرمال‌سازی کلمات و فواصل در `postprocessing/text_formatter.py` ایجاد شد. مدیریت کاراکترها در `utils/dataset.py` قرار گرفت. |
| **مرحله ۹: رابط کاربری و API** | ✅ | سرور `FastAPI` در `api/app.py` و رابط کاربری `Gradio` در `ui.py` نوشته شده و آماده دریافت درخواست هستند. |

---

## 🟡 مراحل نیمه‌تمام (کدهای پایه نوشته شده اما نیازمند تکمیل)

| مرحله در نقشه راه | وضعیت | نیازمندی برای تکمیل |
|:---|:---:|:---|
| **مرحله ۳: تشخیص ناحیه متن** | ⏳ | ساختار شبکه `CRAFT` در `models/detection/craft.py` نوشته شد. **نیاز به:** دانلود وزن‌های آموزش‌دیده (Pretrained) یا شروع آموزش. |
| **مرحله ۴: شناسایی کاراکتر** | ⏳ | ساختار شبکه‌های `CRNN` و `TrOCR` در پوشه `models/recognition/` نوشته شد. **نیاز به:** لود کردن وزن‌های آموزش‌دیده. |
| **مرحله ۷: یکپارچه‌سازی** | ✅ | کلاس `OCREngine` در `inference.py` کامل شد. مدل‌های Mock جایگزین شدند با **Tesseract OCR** واقعی. رابط کامل عملیاتی است. |
| **مرحله ۱۰: بهینه‌سازی فارسی (جدید)** | ✅ | تشخیص خودکار زبان (Persian/English)، دانلود خودکار مدل‌های آموزش‌دیده، PSM تطبیقی، و بهینه‌سازی برای متون راست‌به‌چپ. |

---

## 🔴 مراحل انجام‌نشده (در صف انجام)

| مرحله در نقشه راه | وضعیت | قدم بعدی |
|:---|:---:|:---|
| **بخش آموزش (Training)** | ❌ | نوشتن کدهای فایل `train.py` برای آموزش شبکه‌های `CRAFT` و `CRNN`. |
| **آماده‌سازی داده‌ها** | ❌ | دانلود یا ساخت دیتاست‌های حاوی متون فارسی برای قرار دادن در پوشه `data/`. |
| **مرحله ۸: بهینه‌سازی** | ❌ | اعمال Quantization و تبدیل مدل‌ها به فرمت‌های بهینه (مثل ONNX) پس از اتمام آموزش مدل‌ها. |

---

> **خلاصه وضعیت فعلی:**  
> پروژه در حالت **کاملاً عملیاتی** قرار دارد. کل Pipeline از دریافت تصویر/PDF تا پیش‌پردازش، پردازش OCR، و نمایش رابط کاربری به صورت کامل پیاده‌سازی شده است. 
>
> **نقطه عطف:** استفاده از **Tesseract OCR** به‌عنوان موتور OCR عملیاتی، جایگزین مدل‌های شکست‌خورده CRAFT/CRNN.
>
> **آمادگی فعلی:**
> - ✓ تشخیص خودکار زبان (فارسی/انگلیسی)
> - ✓ دانلود خودکار مدل‌های آموزش‌دیده (fas + eng)
> - ✓ بهینه‌سازی پیش‌پردازش برای متون راست‌به‌چپ
> - ✓ API FastAPI آماده
> - ✓ رابط Gradio فعال
>
> **مسیر توسعه آتی:** تجاری‌سازی با مدل‌های هوش مصنوعی کاستم یا بهبود Tesseract برای دقت بالاتر.
