# گزارش تغییرات پروژه OCR به سمت موتور OCR واقعی

## تاریخ
2026-08-13

## خلاصه تصمیم
پروژه در وضعیت فعلی ساختار کلی OCR را داشت، اما فاقد مدل‌های واقعی و معتبر برای `CRAFT` و `CRNN` بود. بررسی‌ها نشان داد که لینک‌های دانلود مدل‌ها نامعتبر هستند و فایل‌های دانلودشده یا 404 می‌گیرند یا برای `torch.load` با نسخه جدید PyTorch سازگار نیستند.

به همین دلیل تصمیم گرفته شد که پروژه از حالت مدل‌محور و وابسته به وزن‌های ناموجود خارج شود و به سمت موتور OCR واقعی و قابل‌اعتماد با Tesseract حرکت کند.

## دلیل اصلی تغییر
- لینک‌های دانلود `CRAFT` و `CRNN` معتبر نبودند.
- فایل `crnn_weights.pth` اشتباه یا نامتعارف دانلود شده بود.
- PyTorch 2.6 به‌صورت پیش‌فرض `weights_only=True` را فعال می‌کند، بنابراین `torch.load` روی checkpoint‌های نامتعارف یا غیر استاندارد شکست می‌خورد.
- پروژه باید بدون وابستگی به مدل‌های ناموجود، حداقل برای اجرا و OCR عملی، قابل استفاده باشد.

## تغییرات اعمال‌شده

### 1) اضافه شدن backend OCR جدید
فایل جدید ساخته شد:
- `backend/ocr_backend.py`

این فایل یک کلاس `TesseractOCRBackend` دارد که با استفاده از `pytesseract` روی تصویر OCR انجام می‌دهد.

### 2) تغییر تنظیمات پیکربندی
در `configs/config.yaml`:
- بخش `backend: tesseract` اضافه شد
- مسیر مدل‌های قبلی به‌صورت optional در نظر گرفته شد
- تنظیمات زبان به `fas+eng` تغییر یافت

### 3) تغییر در `inference.py`
در این فایل:
- اگر وزن مدل‌های CRAFT/CRNN وجود نداشت، برنامه دیگر fail نمی‌شود.
- به‌جای crash، از موتور OCR Tesseract استفاده می‌شود.
- خروجی `read_image` و `recognize` برای حالت fallback سازگار شده‌اند.

### 4) به‌روزرسانی `ui.py`
رابط Gradio اکنون با وجود نبودن مدل‌ها همچنان می‌تواند OCR انجام دهد و نتیجه را نمایش دهد.

### 5) به‌روزرسانی `requirements.txt`
پکج‌های لازم برای مسیر عملی‌تر اضافه/حفظ شدند، به‌ویژه `pytesseract`.

## مزیت این تصمیم
- پروژه دیگر به مدل‌های نامعتبر وابسته نیست.
- UI و API روی محیط واقعی اجرا می‌شوند.
- برای پردازش اسناد، OCR عملی و پایدار فراهم می‌شود.
- اگر در آینده مدل‌های واقعی CRAFT/CRNN پیدا شوند، در مسیر مدل‌محور هم می‌توان دوباره بازگشت کرد.

## هشدار
برای استفاده از Tesseract، باید Tesseract OCR روی سیستم نصب باشد. در ویندوز معمولاً باید از نصب رسمی Tesseract استفاده شود و مسیر آن به PATH اضافه شود.

## نتیجه نهایی
پروژه از حالت "Mock/ناموفق مدل‌محور" به حالت "قابل اجرا و عملی" منتقل شد؛ بدون اینکه به لینک‌های خراب و مدل‌های نامعتبر وابسته بماند.
