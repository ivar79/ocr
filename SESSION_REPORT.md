# گزارش جلسه بهینه‌سازی OCR فارسی

**تاریخ:** 2026-08-13  
**وضعیت:** ✅ تکمیل‌شده  
**نسخه:** 1.0

---

## خلاصه جلسه

این جلسه به بررسی و بهینه‌سازی سیستم OCR فارسی برای بهبود دقت تشخیص متن اختصاص داشت. تمرکز اصلی روی تنظیمات Tesseract و پیش‌پردازش تصاویر برای متن فارسی بود.

---

## کارهای انجام‌شده

### 1. بررسی پروژه
- خواندن فایل‌های مارک‌داون پروژه (README.md, QUICK_START_FA.md, STATUS.md, roadmap.md)
- بررسی وضعیت فعلی پروژه و نقشه راه آینده
- تأیید عملیاتی بودن سیستم با موتور Tesseract OCR

### 2. تست اولیه سیستم
- اجرای رابط کاربری Gradio
- تست با تصویر واقعی فارسی (سایت مبلمان)
- ثبت نتایج: دقت پایین OCR برای متن فارسی

### 3. بهینه‌سازی اولیه
**تغییرات در `backend/ocr_backend.py`:**
- اصلاح تشخیص زبان: تغییر از تشخیص خودکار نادرست به استفاده همیشگی از `fas+eng`
- بهبود PSM: تغییر به PSM 6 برای متن فارسی

**تغییرات در `preprocessing/image_processing.py`:**
- تغییر thresholding از Adaptive به Otsu برای حفظ اتصالات حروف فارسی
- افزایش CLAHE clip limit از 2.0 به 3.0
- اضافه کردن morphological cleanup بسیار ملایم

### 4. تست دوم
- ریستارت سرویس با تغییرات جدید
- تست مجدد: کیفیت بهبود یافت اما هنوز جای پیشرفت داشت

### 5. بهینه‌سازی بیشتر (ناموفق)
**تغییرات اضافی:**
- تغییر PSM به 3 با پارامتر preserve_interword_spaces
- تغییر CLAHE tileGridSize به (4,4)
- بازگشت به Adaptive thresholding با پارامترهای جدید
- اضافه کردن auto-invert

**نتیجه:** کیفیت کار افت کرد

### 6. بازگشت به حالت بهتر
- برگرداندن تغییرات به حالت قبلی که کیفیت بهتر بود
- حفظ تنظیمات:
  - PSM 6
  - Otsu thresholding
  - CLAHE clipLimit=3.0, tileGridSize=(8,8)
  - Morphological cleanup ملایم

---

## تنظیمات نهایی (بهترین حالت)

### Tesseract Config
```python
# Language: fas+eng
# PSM: 6 (Single uniform block of text)
# OEM: 3 (Default)
config = "--oem 3 --psm 6"
```

### Preprocessing Pipeline
1. Grayscale conversion
2. Upscaling (2x)
3. Bilateral filter denoising
4. CLAHE enhancement (clipLimit=3.0, tileGridSize=(8,8))
5. Otsu thresholding
6. Deskewing
7. Mild morphological cleanup

---

## نتایج تست

### تست اولیه (قبل از بهینه‌سازی)
- **وضعیت:** دقت پایین
- **مشکل:** متن فارسی خراب و غیرقابل‌خواندن

### تست دوم (بعد از بهینه‌سازی اولیه)
- **وضعیت:** بهبود قابل‌توجه
- **نتیجه:** متن قابل‌خواندن اما هنوز خطا وجود دارد

### تست سوم (بعد از بهینه‌سازی بیشتر)
- **وضعیت:** افت کیفیت
- **مشکل:** تغییرات پارامترها نتیجه معکوس داشت

### تست نهایی (بازگشت به حالت بهتر)
- **وضعیت:** کیفیت قابل‌قبول
- **نتیجه:** بهترین حالت در این جلسه

---

## فایل‌های اصلاح‌شده

1. **backend/ocr_backend.py**
   - بهبود تشخیص زبان
   - تنظیم PSM بهینه برای فارسی

2. **preprocessing/image_processing.py**
   - بهینه‌سازی thresholding
   - تنظیم CLAHE برای متن فارسی
   - اضافه کردن morphological cleanup ملایم

---

## پیشنهادات برای آینده

### کوتاه‌مدت
1. تست با PSM های مختلف (1, 3, 6, 13) برای پیدا کردن بهترین حالت
2. بهینه‌سازی پارامترهای CLAHE (clipLimit و tileGridSize)
3. آزمایش با روش‌های مختلف thresholding

### بلند‌مدت
1. آموزش مدل Tesseract با داده‌های فارسی
2. پیاده‌سازی شبکه‌های عمیق (CRNN, CRAFT)
3. بهبود postprocessing با مدل زبانی

---

## وضعیت پروژه

- ✅ سیستم عملیاتی و آماده استفاده
- ✅ رابط کاربری Gradio فعال
- ✅ API FastAPI آماده
- ⏳ نیاز به بهینه‌سازی بیشتر برای دقت بالاتر

---

## دسترسی به سیستم

- **رابط کاربری:** http://127.0.0.1:7860
- **API:** http://localhost:8000 (با اجرای python api/app.py)
- **مستندات:** QUICK_START_FA.md

---

**آخرین بروز‌رسانی:** 2026-08-13  
**نسخه:** 1.0  
**نویسنده:** OCR Project Team
